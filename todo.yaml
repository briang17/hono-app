

apps:
  openhouse:
    - When organization already exists, frontend doesn't handle error.
    - When trying to register with existing email, frontend doesn't handle error.
    - Top bar with logout and organization options for admin role.
    - See agent list, manage FUB credentials to send leads
  api:
    - Login errors when user not found, instead of handling and communicating?
    - 2026-02-27T00:26:57.986Z ERROR [Better Auth]: User not found {
        email: "integrations@anew.house",
      }
    - Login should wait in all cases. 2s to 3s. prevent timing attacks.

    - emailService with resend, need to finalize implementation

    - in order to add user invitation, and finally flesh out organization
    
    - implement VALUE OBJECTS!!!

packages:
workers:
